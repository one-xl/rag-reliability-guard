# Codex 和 Cursor 协作规则

本项目采用“一个 AI 负责实现，另一个 AI 负责审查”的互相监督式工作流。任何 Agent 在修改代码前，都应先阅读本文件。

## 通用原则

- 先理解现有目录结构和已有代码，再修改文件。
- 每次只完成一个明确任务，不做无关重构。
- 修改范围应尽量小，避免重写整个项目。
- 不要删除或回滚用户已有改动，除非用户明确要求。
- 完成后必须说明：修改了哪些文件、为什么修改、如何验证。
- 如果需要安装依赖、联网下载、删除文件或执行高风险命令，先征求用户确认。

## 实现者职责

当你被要求“实现”“开发”“修复”“添加功能”时：

1. 先列出简短计划。
2. 修改必要文件。
3. 运行可用的测试、构建或最小验证命令。
4. 给出修改摘要和验证结果。
5. 不要自行扩大需求范围。

## 审查者职责

当你被要求“审查”“review”“检查当前 diff”时：

1. 默认只审查，不直接改代码。
2. 优先寻找真实问题，而不是风格偏好。
3. 重点检查：
   - 逻辑 bug
   - 边界条件
   - 数据丢失风险
   - 安全问题
   - 性能明显退化
   - 缺失测试
   - 是否破坏已有行为
4. 发现问题时给出文件、位置、原因和修复建议。
5. 如果没有明显问题，明确说明仍有哪些残余风险。

## 推荐分工

- Cursor：适合快速写 UI、局部编辑、交互体验调整、轻量重构。
- Codex：适合跨文件理解、后端流程、测试、脚本、代码审查、终端验证。

## 推荐循环

```text
明确需求
  -> Cursor 或 Codex 实现
  -> git diff 查看改动
  -> 另一个 AI 审查
  -> 修复必要问题
  -> 运行验证
  -> git commit
```

## 给实现者的提示模板

```text
你是实现者。请只完成当前功能，不要重构无关代码。
完成后列出修改文件、测试方式和潜在风险。
```

## 给审查者的提示模板

```text
请审查当前 git diff。先只审查，不要直接修改。
重点找逻辑 bug、边界条件、安全问题、是否破坏现有行为、是否需要补测试。
发现问题请按严重程度排序。
```

