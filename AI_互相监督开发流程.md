# Codex 和 Cursor 互相监督开发流程

这份流程用于在本地项目中同时使用 Codex 和 Cursor，避免两个 AI 互相覆盖代码。

## 1. 每个任务先建分支

```bash
git switch -c feature/task-name
```

示例：

```bash
git switch -c feature/rag-upload
```

## 2. 选择一个实现者

适合让 Cursor 先做：

- 页面布局
- 表单交互
- 小范围前端组件
- 局部代码补全

适合让 Codex 先做：

- 后端接口
- RAG 流程
- 文件解析
- 向量检索
- 评测脚本
- 测试和构建问题

## 3. 实现提示词

```text
你是实现者。请只完成当前功能，不要重构无关代码。
先看项目结构和现有实现，再修改必要文件。
完成后列出修改文件、测试方式和潜在风险。
```

## 4. 查看改动

```bash
git status --short
git diff
```

## 5. 让另一个 AI 审查

```text
请审查当前 git diff。先只审查，不要直接修改。
重点找：
1. 逻辑 bug
2. 边界条件
3. 安全问题
4. 是否破坏现有行为
5. 是否需要补测试

发现问题请按严重程度排序；如果没有阻塞问题，请说明剩余风险。
```

## 6. 修复必要问题

如果审查发现问题，再让实现者或审查者修复：

```text
请只修复审查中指出的必要问题，不要做无关重构。
修复后运行可用验证命令，并说明结果。
```

## 7. 提交

```bash
git add .
git commit -m "Describe the completed task"
```

## 8. 注意事项

- 不要让 Codex 和 Cursor 同时修改同一个文件。
- 不要在未提交的情况下连续让两个 AI 大范围改代码。
- 每完成一个稳定阶段就提交一次。
- 任何删除文件、清理目录、重置分支的操作都要先确认。

