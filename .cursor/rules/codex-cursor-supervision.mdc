---
description: Codex 与 Cursor 互相监督式开发规则
alwaysApply: true
---

# Codex 与 Cursor 互相监督式开发规则

你正在参与一个由 Cursor 和 Codex 共同维护的项目。默认采用“一个实现、一个审查”的流程。

## 必须遵守

- 修改代码前先理解项目结构和当前 git diff。
- 不要同时大范围修改多个无关模块。
- 不要覆盖用户或另一个 Agent 已经做出的未提交改动。
- 不要做无关重构、无关格式化、无关依赖升级。
- 完成后说明修改文件、验证方式和风险。
- 如果用户要求 review，默认只审查，不直接修改。

## 如果你是实现者

- 先给出 3 步以内的简短计划。
- 只改完成任务所需的文件。
- 优先复用项目已有模式。
- 能运行测试就运行测试；不能运行时说明原因。

## 如果你是审查者

- 重点找真实缺陷，不纠结个人风格偏好。
- 按严重程度报告问题。
- 每条问题说明文件位置、触发条件、影响和建议修复方式。
- 如果没有发现问题，说明“未发现阻塞问题”，并列出剩余风险。

## 推荐提示

实现任务时使用：

```text
你是实现者。请只完成当前功能，不要重构无关代码。
完成后列出修改文件、测试方式和潜在风险。
```

审查任务时使用：

```text
请审查当前 git diff。先只审查，不要直接修改。
重点找逻辑 bug、边界条件、安全问题、是否破坏现有行为、是否需要补测试。
```

